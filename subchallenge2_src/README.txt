We expect the following dependencies for the code to execute
MATLAB
the path of the matlab should be set so that it is directly accessible using matlab command from UNIX terminal

use python 2.7,python 3 might create issues
PYTHON
install package python-numpy, python-networkx

download the network files and put it in data/subchallenge2/
Then run bash subchallenge2.sh

